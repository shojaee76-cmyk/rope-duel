# THE ROPE DUEL (rope-duel)

## What it is
A single-page site: two Andalusian cats (SULTAN BIGOTES = BUY pressure, green; DON GATO = SELL pressure, red) duel with swords on a suspended rope in a moonlit courtyard. The fight is driven by a live BTC/USDT tape from a public exchange WebSocket: 30s rolling order-flow pressure pushes attacks/parries, the rope flag shows live price + 24h change, whales trigger callouts, crowd cheers the winning side, and a 1s candlestick panel is mounted on the courtyard wall. The feed is MULTI-PROVIDER (Binance first, Bybit as the geo-reachable fallback) and the HUD names the one in use (`LIVE · BINANCE` / `LIVE · BYBIT`). Falls back to a SIMULATION tape (orange badge) only when no provider is reachable, so the page always works.

- Origin: kanban task `t_02c9f4fe` (inrange board): "a fun webpage shows btc/usdt price".
- Live URL: https://shojaee76-cmyk.github.io/rope-duel/
- Source repo: https://github.com/shojaee76-cmyk/rope-duel (public, MIT not set)
- Local durable copy: `C:\Users\capit\rope-duel` (this dir)
- v20 status: LIVE CHARACTER POLISH (rounded feline faces, embroidered costumes, shaped hats; deployment verified). v19 retained: READABILITY PASS (slower, legible swordplay with peak HOLDS; the defender really SITS in his sword guard; the pair's left/right travel is driven by the BTC tape via a PRICE LANE; the chart is a mounted screen standing proud of the board on brass pins; richer background textures + two hung wall carpets; tails raked behind and up). New checkers: tools/lanecheck.mjs, tools/rugcheck.mjs, tools/bladeprobe.mjs, tools/audit.mjs, tools/poselab.mjs, tools/wallshot.mjs.

## Architecture
- `feed.js` — BtcTradeFeed: Binance WS (trade + ticker), 30s rolling pressure [-1,+1], 250ms throttled emits, 3-endpoint reconnect w/ backoff, deterministic sim fallback. Zero deps.
- `src/scene.js` — `createDuelScene(container, {demo, debug})`: verlet rope, 10 signature/combat states, blade-lock + clash adjudication, hit reels, price flag, VFX, crowd. three r186 (vendored in repo; bundled into bundle.js locally).
- `src/tex.js` — procedural texture factory: fur (patches / irregular tabby), cloth weave, brushed metal, leather, stone ashlar, braided hemp. Each returns `{map, bump[, rough]}`; every texture gets mipmaps + anisotropy.
- `src/director.js` — fight director: pair locomotion (circle → engage → break), tempo clock, move table + cooldowns, lock/clash/hit resolution.
- `main.js` + `feed-adapter.js` — integration boot: feed onChange payload -> scene API + HUD.
- `index.html` — HUD, pressure meter, whale callouts, connection badge. Only 3 runtime files needed: index.html + feed.js + bundle.js (classic IIFE, works over file://).
- `tools/` — verification harness: `errcheck.mjs` (boot errors), `probe.mjs` (30s fight/rope/perf telemetry), `ropelab.mjs` (rope geometry + isolated render + texture-vs-flat detail), `ropegone.mjs` (frozen-frame A/B: are the rope pixels there), `shot.mjs` (screenshots at real combat beats), `livecheck.mjs` (deployed build: hash match, errors, live-tape fight stats), `eyes.py` (OpenRouter vision art-director pass).

## Status: LIVE v21b - black ball eyes, faster fight, the guard blocks sword on sword (2026-09-17)
- Kanban root `t_02c9f4fe` + children `t_c027c590` (research), `t_a1985f86` (design spec), `t_f39e467f` (scene), `t_41492ad4` (feed), `t_e2039773` (integration) all done.
- Verification: `verify_integration.mjs` 17/17 PASS, 0 console errors; live-URL headless Chrome pass: HUD 78,865.98 / 232 trades-s / badge OPEN / 0 errors; pixel assert 15% gold torchlight, 13% ivory, lum 0-231.
- Kanban artifacts (screenshots, zips, reports) survive in `%LOCALAPPDATA%\hermes\kanban\boards\inrange\attachments\`.
- New frame-budget + contact harness in `tools/`: `fightprof.mjs` (per-subsystem frame time bucketed idle/active/brawl + GPU timer query), `screen_gap.mjs` (on-screen head clearance in pixels), `govtest.mjs` + `restest.mjs` (quality-governor evidence), `liveload.mjs` (callout storm + forced brawl), `headdiag.mjs`/`triple_probe.mjs` (contact-constraint telemetry), `shot_meter.mjs`/`meter_ab.mjs` (badge geometry + placement A/B).

## Progress log
- 2026-09-17 (**v21b: plain black ball eyes**). User: *"Make eyes black balls"*. The eye material in `buildRig` is now one flat near-black glossy ball (`#0A0A0D`, roughness .42, a whisper of self-emission so the silhouette survives when the head turns into its own shadow) and the ballEyeTexture map is no longer attached. No iris, no slit, no pupil mesh, no catchlight, no lid, no brow: each eye is a single sphere with one material. `ballEyeTexture()` is left in costume-textures.js, documented as unused, so the slit version is a one-line restore.
  - Verified by pixels, not by eye: `tools/eyeprobe.mjs` now hides the balls and compares the mean luminance of a box on each eye's projected centre. Close duel-angle camera: the eye box goes 150 -> 47 (cat A) and 199 -> 49 (cat B), i.e. the eye is far darker than the head it sits on; at the real gameplay camera the box still drops 19-30 per eye, so the black dot reads at duel distance too. ALL PASS on both cats at both cameras, 0 page errors.
  - Blind vision pass on the head close-up: "the eyes are black, plain black balls... no obvious ugly or broken elements like clipping, floating parts or holes. The eyes sit well within the head."
  - Regression on the new bundle: errcheck 0 errors; clipcheck ALL PASS (blade into foe 2.6%, worst 0.131); rolecheck ALL PASS; identitycheck ALL PASS; wallboardcheck ALL PASS; verify_ui ALL PASS; costumecheck passed; probe 77 fps, 89.8 attacks/min, 0 errors.
- 2026-09-17 (**v21: ball eyes back, faster fight, the guard blocks**). Three user directives in one pass: *"the eyes are getting fucked. Make them simple ball and back to the original eye. Its ugly now"*, *"also the movement are too low now. Make it faster"*, *"in defend, make she sword block the other sword"*.
  1. **Eyes: the v20 almond stack is gone, the eye is a ball again.** Removed the convex almond socket + painted-iris almond + stroke eyelid + stroke brow, the `almond()` geometry, the `eyeTexture()` map and the `socket`/`lid`/`brow`/`pupil`/`glint` materials. Each eye is now ONE sphere (`eye-ball`) whose own texture (`ballEyeTexture`, new in costume-textures.js) carries the iris, the slit pupil and the catchlight. The pupil is painted where a three.js sphere maps its +Z pole (u=.25, v=.5), so it sits on the face of the ball and follows its curvature: nothing in the eye can protrude, float, clip or z-fight, which is exactly what "simple ball" has to mean in 3D.
     - Two mesured corrections on top of the v19b ball: the v19b pupil box (x .158 vs a .165 ball surface) was buried INSIDE the eyeball, so those eyes were plain colour orbs with no gaze at all; and the ball was yawed 44 deg outward with its centre outside the skull, which the vision pass called "googly side-mounted orbs" (4/10 for the v20 eyes, 1/10 for the v19b orbs). The ball is now at yaw .42 rad, 0.027 proud of the skull, and the slit is 11 of 128 texels wide so it survives the duel camera. Blind vision check on a 4x crop: "a dark vertical slit is visible, it reads as a cat's eye, 7/10".
     - Verification: `tools/eyeprobe.mjs` (new) A/Bs the eye map ON vs OFF on the pinned pair and requires the extra dark pixels the slit+rim must paint - both cats pass at both the real gameplay camera and a close duel-angle camera. Dead ends recorded in the tool so they are not repeated: frame-diffing is useless (the scene animates), and absolute RGB tests on a painted part fail because the night key light washes a saturated material toward white (flat green renders rgb(211,244,164)).
  2. **Tempo: every move is 1.35x shorter again.** The v19 readability pass stretched the moves ~1.7x to kill the blur, which overshot into slow motion. `director.js` MOVES lengths /1.35 (LUNGE 0.90->0.67, RUSH 1.05->0.78, SLASH_UP 1.00->0.74, THRUST 0.72->0.53, SLASH_SPIN 1.10->0.81, RIPOSTE 1.05->0.78, PARRY_HOP 0.75->0.56, PARRY_BEAT 0.70->0.52, FEINT 0.85->0.63, TAUNT 1.30->0.96), cooldowns /1.3, HOLD trimmed with them, the beat clock 1.05/1.55 -> 0.78/1.15, the side breather 0.75 -> 0.55, the lock 0.50-0.62 s + 0.70 -> 0.42-0.52 s + 0.55, the pair's locomotion bar 2.4 -> 1.9 s and its approach rates 2.6/1.8 -> 3.2/2.2, per-move RECOVER lengths /1.3 in cats.js, and the pose tracking constant 20 -> 26 so the limbs keep up with the shorter states. Measured: `tools/probe.mjs` 89.9-97.9 attacks/min (v19 was 81.9, v18 128), 76 fps, 278 draw calls, 0 errors; `tools/bladeprobe.mjs` a cut still spans LUNGE 35 / SLASH_UP 45 / THRUST 37 frames (0.21-0.23 s, v19 0.23-0.27 s), so the phrase is ~26% shorter while each swing stays legible.
  3. **The guard now really blocks: blade on blade.** Two changes. `director.js`: an attack that lands on a SEATED guard is a block, not a coin flip - the lock odds go from 0.6 to 0.85 for SIT_GUARD, so the crossed-blades beat arrives far more often while he is seated. `scene.js`: a new **BLADE GUARD** constraint (same monotone-verified pattern as the head and blade-contact constraints, `?block=off` disables it) measures the REAL rendered blade segments (4 samples each, hilt excluded) while a cat is in SIT_GUARD / PARRY_BEAT / PARRY_HOP, then hill-climbs his shoulder.z, shoulder.x and elbow.z toward contact, keeping a step only if the measured blade-to-blade distance actually drops AND the blade gains no penetration into either body. The seated guard pose also rides higher and further forward (shS_z 0.70->0.88, shS_x 0.35->0.50, elS -1.45->-1.25) so the blade sits ON the incoming line rather than beside it.
     - Measured on the pinned pair (`tools/eyeguard.mjs`, new): guarding blade to attacking blade LUNGE 0.17 -> 0.057, THRUST 0.95 -> 0.046, RUSH 0.19 -> 0.12, guard-to-guard 0.73 -> 0.10 world units; crossing points sit mid-blade (e.g. LUNGE at samples [3,6]), not tip-to-tip. SLASH_UP stays ~1.1 apart: that cut arrives from above, out of the seated cat's reach, and the constraint deliberately does not chase it.
     - Blind A/B on the same frame: the vision pass says of the BEFORE frame "No, the blades are roughly 1-2 inches apart" and of the AFTER frame "The two swords are crossing each other".
     - Safety: `tools/clipcheck.mjs` ALL PASS (blade into foe 1.9% of samples, worst 0.128, inside the <=8% / <=0.15 gates; 0 self-clip, 0 body, 0 head, head min 0.800).
  - Full regression on the final bundle: errcheck 0 errors; clipcheck ALL PASS; rolecheck ALL PASS (seated 23-24/32 both directions, role ownership exact); lanecheck ALL PASS; wallboardcheck ALL PASS; verify_ui ALL PASS (12.0 ms avg frame at 1280x800, no overflow 1280/768/390/320); identitycheck ALL PASS; verify_colors BUY green rgb(53,208,127) / SELL red rgb(239,83,80) intact; costumecheck passed (both characters render); probe 0 errors / 76 fps / 278 calls.
  - New tools: `tools/eyeguard.mjs` (pins the pair, forces the defender's guard against each strike, measures blade-to-blade and writes close-up shots), `tools/eyeprobe.mjs` (the eye-visibility A/B). Evidence: `tools/shots/v21*`, `v21_eyes_compare.png`, `v21_block_compare.png`.
- 2026-09-16 (**v18.2: eyes, cape deletion, forward guard**). Three user complaints, all vision-confirmed on the deployed build and all fixed:
  1. **"you fucked up the eyes"**: eyeTexture() painted the iris gradient on a dark #211B19 background, but the almond geometry maps its full UV square, so the gradient circle only covered the centre - dark corners at the almond tips made both cats' eyes read as black blobs. Fix: canvas base is now the iris colour itself and the gradient fills the whole 128px field (r 96), so every visible texel is iris; slit + highlights unchanged.
  2. **"the cloak of the spanish knight is colliding with the cat body. If you cant fix it, delete it"**: the cape was a flat waved sheet whose top edge sat buried inside the golilla ruff cone and which the raised/wrapped tail (v19) whipped straight through; a cloth sheet without cloth collision cannot be kept clip-free against an animated rig, and the user sanctioned deletion. buildCape() + its spine attachment removed (gold collar kept as the armour back-strap). Don Gato keeps the Spanish read via morion + plume, golilla ruff, red slashed doublet + tabard, tassets, baldric, rapier.
  3. **"When the cat is holding ground, he is gonna put his sword in front of him. Not behind. Thats not make sense"**: the v19b SIT_GUARD pose (shS_z -0.25, elS -2.55) put the blade at -0.94 in spine space = pointing BACKWARD over his shoulder. Fix measured live: shS_z +0.70 (arm swung forward), elS -1.45 (forearm up-forward) -> tip rides in the air corridor between the cats beside the foe's head. tools/clipcheck.mjs 24 s: 0 blade-into-foe samples across 988 SIT_GUARD frames (worst overall 0.116 on a SLASH_SPIN, inside gates), 0 self-clip, 0 body/head overlap. Runtime pose-overrides during sweeps must use swallowing property setters - plain target writes are stomped by the state handler each frame.
  - Verification: eyes/cape/sword all re-confirmed by eyes.py on the fixed build (cape absent, eyes readable, sword forward); clipcheck ALL PASS; verify_chart (incl. live), rolecheck, identitycheck, verify_ui, mock-live, wallboardcheck ALL PASS; errcheck 0 errors; probe 79.9 apm / gapMin 0.81 (demo tape mix varies apm run to run; steadiness unchanged). Commit 9164645, deploycheck DEPLOY VERIFIED.
- 2026-09-16 (**v20: cat and costume polish**, LIVE, independently reviewed and deployment verified at commit aac1ae1). Preserved the selected Sultan/Spanish knight identities and BUY green/SELL red mapping. Rounded paired muzzle pads and chin replace the box snout; convex almond eyes use painted slit pupils and catchlights; curved tapered whiskers replace straight rods. Sultan has inward-sculpted linen folds and green geometric embroidery on robe, sleeves and placket, with overlapping turban wraps. Don has a curved morion comb, shaped brim, feather plume, embroidered red cape/doublet and armour edging. New `src/costume-textures.js` paints deterministic maps locally without external assets. Animation-layer text remains identical to v19b, including seated robe hitch. Facial detail radius <=0.295 and skirt radius <=0.311 reported by construction tests. Combined character draws 179 -> 174, triangles 25,568 -> 32,574. Parent rebuilt bundle and verified actual rendered character pixels with new `tools/costumecheck.mjs`, plus neutral-lit portraits (not concept art). Regression: clipcheck passes (2% blade sample contacts, 0 self/body/head penetration, 0.80 minimum head clearance); identity, role, wallboard, UI and pose suites pass with zero page errors. 30s render probe: 18.9ms average frame, 301 draw calls, 38,762 triangles. This is one local run, not a controlled performance improvement claim. Review rated faces feline/readable and embroidery visible; close-up fur/boot texture quality remains modest. Evidence: `tools/shots/costume-v20*` including `costume-live-v20-report.json`. Deployed bundle hash matches local (7462a614ef1e); real feed OPEN, ticker cross-check 0.000% deviation, zero page errors. Live character toggle tests confirm both rendered (18,728 / 20,633 changed pixels). Portrait-only module imports are local-only because production ships the IIFE bundle.
- 2026-09-16 (**v19b: two reported regressions + the endless clipping** - user: "it got little blur when im running it", "and the chart is not visible anymore", "also the swords, the cats and the clouthes are colliding to each other anytime. fix that also").
  1. **The chart was genuinely invisible: the screen body occluded the glass.** v19 added a bezel + screen body to the tape mount and centred the body at `z - FRAME_D*0.22`; with a 0.055 depth its FRONT face landed at `z + 0.0055`, i.e. 0.0055 *in front* of the glass plane. The body is opaque and depth-writes; the glass is transparent and only depth-tests, so the body won every comparison and the chart was never drawn. Measured with a one-mesh A/B: hiding that single box changed **72% of the board rect's pixels**. Fix: the body centre is now `z - FRAME_D*0.85` (front face 0.0575 behind the glass). Proof: toggling the GLASS itself now changes **82%** of the rect with bright pixels 31928 on vs 27385 off, and `chart.setVisible()` was added so the A/B is repeatable.
  2. **Why v19 shipped a blind chart: two checkers that certified the bug.** `stats().ink` reads the CANVAS (always painted, occlusion or not) and wallboardcheck's "board shows green candle / bright text pixels" classes landed on other content inside the rect (the brass stand-offs, the lit bezel, torch glow) with counts that barely moved from v18 to v19 - a 159 -> 172 px "candle" count was the tell. LESSON, now enforced: a claim that something is ON SCREEN must be proven by RENDERED pixels changing when that thing is toggled, never by canvas ink or by a colour histogram over a rect that contains other objects. `setVisible` + the A/B diff is that proof; the old pixel classes stay only as a smoke test.
  3. **The blur was the quality governor, and it is now floored.** Measured: the governor stepped the render ratio to 0.9 in the sandbox, and `RATIO_STEPS` allowed 0.5 - on a 1x display that is a 25-50% linear resolution cut, i.e. visible softness ("everything equally soft with stepped edges"). Four changes in `scene.js`: a **warm-up grace period** (the first 150 frames include shader compiles and every texture upload, and their frame times alone used to trigger a step at boot); the trigger moved from >20 ms (below 50 fps) to >26 ms (below ~38 fps) and now needs **two consecutive** slow windows; the step set is [1, 0.9, 0.82, 0.75] instead of [1, 0.85, 0.72, 0.6, 0.5]; and a **hard sharpness floor**: `MIN_RATIO = 1.0` CSS pixel on desktop, enforced by capping the reachable step index (`STEP_CAP`), so at base 1.0 the governor cannot reduce resolution at all and at base 1.75 (a 2x display) it can only fall to 1.31 - still sharper than 1:1. Verified: `quality()` reports `ratio 1, step 0, changes 0` through a 25 s run in the same sandbox that previously stepped down. The desktop base cap also moved 1.5 -> 1.75, because on a 2x display a 1.5x canvas is stretched by the browser and reads soft even when nothing is wrong.
  4. **The clipping, measured before it was touched** (new `tools/clipcheck.mjs`): with the blade sampled from the guard forward (u >= 0.6 of the sword, so the hilt held against the body is not counted), the blade crossed the FOE's torso capsule on **30.6% of samples, worst 0.18 world units deep** (a 0.26-radius torso); the two cats' bodies never overlapped and the heads never overlapped. Attribution by attacker state put the worst case in **SIT_GUARD (108 samples, 0.18)** - the v19 seated guard was authored as `shS_z 1.45 / elS -1.35`, which swings the upper arm FORWARD with the elbow folded, and since the sword runs along the elbow's +x the blade pointed DOWN in front of the pair. Two fixes: (a) the guard now raises the blade vertically BESIDE him (`shS_z -0.25`, `elS -2.55`, arm carried out of the body plane by `shS_x 0.85`), which is both a clearer high guard and clear of both bodies; (b) a new **BLADE CONTACT constraint** in scene.js, the same pattern as the v10 head constraint: it samples 4 real blade points, measures penetration into the foe's torso capsule / head sphere AND into the wielder's own, then applies candidate corrections (fold the arm back, carry it out of plane, close the elbow) and KEEPS a step only if the measured penetration actually drops, so it can never make a pose worse. Telemetry: 2912 cat-frames checked, 93 needed a correction, worst input penetration 0.180 -> worst output 0.105. Result: blade into the foe **30.6% -> 2.0%** of samples (worst 0.13), blade into the wielder 0%, bodies 0%, heads 0.80 min, coat overlap 0%. Gates added to clipcheck (<= 8% and worst <= 0.15) so this cannot silently regress.
  - Verification: errcheck 0 errors; clipcheck ALL PASS; rolecheck ALL PASS (the rebuilt guard still seats: 29/32 samples under a leaning tape); lanecheck ALL PASS; wallboardcheck 18/18 incl. the mount contract; verify_ui 21/21; verify_colors / identitycheck / verify_chart ALL PASS.
- 2026-09-16 (**v19: the readability pass** - six user directives in one build: "the fighting cats sword movement is too fast and not recognizable"; "the defending cat still does not sit and defend with his sword. i want that"; "the cats movement to the left and right must be connect to the price of btc"; "the chart is still embeded infused to the board behind it and they collide to each other"; "enhance the texture and the pictures of background"; "put the tail of the cats behind them not down"; plus "if you think of any enhancment on this design and project, do it").
  1. **The sword was a blur.** Measured cause (`tools/bladeprobe.mjs`, new): a v14/v18 strike was 0.40-0.62 s long with the swing arc packed into ~40% of it, so a cut crossed its whole arc in ~0.16 s = **~10 frames at 60 fps**, and the windmill every move ended with spun 2.6 PI in 0.16-0.30 s (up to 50 rad/s of wrist). Three changes, all in `director.js` MOVES + `cats.js swingPhases()`: every move is 1.7-1.8x longer (LUNGE 0.50->0.90, SLASH_UP 0.55->1.00, THRUST 0.42->0.72, RUSH 0.62->1.05, SLASH_SPIN 0.60->1.10, RIPOSTE 0.60->1.05, PARRY_HOP 0.45->0.75, PARRY_BEAT 0.40->0.70, FEINT 0.50->0.85, TAUNT 0.80->1.30); every strike is re-cut as **windup -> swing -> HOLD -> settle** where the HOLD (0.10-0.20 s, from the new HOLD table, handed to the pose layer in `state.data.hold`) freezes the pose at the top of the arc; and the pose smoothing constant drops 26 -> 20 so a limb carries weight instead of snapping between keyframes. Cooldowns (1.6-3.6 s), the side breather (0.55 -> 0.75 s), the beat clock (0.56/0.80 -> 1.05/1.55 s), the lock (0.50-0.62 s + 0.70 s breather) and the RECOVER windmill lengths all scale with them, so the fight is a slower continuous exchange rather than a slower version of the same blur. Measured after: the swing spans **26-30 frames / 0.25-0.35 s** per cut, and the blade then sits quiet at/near its peak for 0.4-0.65 s of the move. The v18 SLASH_SPIN windmill also ended 10 rad away from the next phase's angle, so the pose lerp unwound all of it in ~0.2 s (a second, opposite blur) - it is now ONE revolution ending congruent with the first cut. `_recover`'s arm sweep 2.6 PI -> 1.6 PI.
  2. **The defender now really SITS.** The v18 pose existed but was unreachable and invisible, two separate reasons, both measured. (a) REACHABILITY: `_tempoTick` ran BEFORE `_maintainGuard` and its fallback branch handed the defender a PARRY_HOP every slot the attacker could not use, so the defender was permanently "between parries" (seat present in ~22/32 samples, never in a row). The guard now runs FIRST (update order swapped) and the tempo fallback only seats him; entry threshold |pS| 0.10 -> 0.07; the posture is RENEWED 0.6 s before it lapses so the seat is continuous, and released deliberately on a role flip / flat tape / a gap over 2.7. (b) VISIBILITY: the pose only crouched the root 0.30, and the planted-paw pass in `_applyPose` (clamped +0.18) cancelled most of it, while the dishdashah skirt hangs 0.435 below the hips (i.e. past the rope) so the whole sit disappeared inside a bell of cloth. The seat is now THE LEG FOLD (th 1.25 / kn -2.60, solved so the planted-paw correction lands at ~0), which drops the hips from 0.50 to **0.22 world units above the rope** with the paws ON the rope, and a seated cat hitches his robe (skirt/hem scale + offset eased in and out) plus raises the cape. Both cats did it on the live tape in the mid-build audit (A seated while B feinted). `tools/rolecheck.mjs`: seated 29/32 samples under a leaning tape, ALL ROLE CHECKS PASS.
  3. **The left/right travel IS the price.** (`src/director.js`, new PRICE LANE.) The pair's place on the rope used to drift with the 1-minute trend at 0.5 units/s behind a clamp: a private random walk with no visible cause. It is now the tape: the 30 s price change, scaled so **0.35% = full deflection**, low-passed at 0.7 s over +-3.2 world units, positive = toward the BUY pole. The camera follows only 0.62 of the pair's travel (`CAM_FOLLOW` in scene.js) so the slide is VISIBLE instead of being cancelled by the pan. New `tools/lanecheck.mjs` seeds the director's own 30 s window and asserts from the live scene graph: +0.35% -> lane +0.88, mid +2.82 (A +3.35 / B +2.30); -0.35% -> lane -0.97, mid -3.10; +0.175% -> lane +0.39 (proportional); and the pair's projected screen x moves **36.5% of the frame width** between the two extremes. The HUD now carries a PRICE LANE scale (bottom centre: a labelled track with a gold marker for the pair and a tinted dot for EACH cat, plus the live 30 s change), so the cause of the movement is on screen instead of being invisible.
  4. **The chart is a mounted screen, not a decal.** Measured cause: the chart plane was placed at `board.z + 0.09`, which is EXACTLY the board's front face (the box is 0.18 deep), so the glass and the walnut were coplanar and z-fought - the old `wallboardcheck` assertion ("plane within 0.02 of the board face") was literally certifying the bug. Now `arena.js` exports the mount stack (`face` / `gap` / `screenZ`), builds four brass stand-offs spanning the 0.13 air gap plus a cast shadow on the board below the screen, and `skychart.js` builds a real 0.10-deep bezel (four rails + a screen body) around a glass that now sits **0.18 proud of the board face**, at 0.88 of the board's width so the walnut reads as a frame. The canvas is supersampled 1.35x -> 1.9x (the review called the header/axis digits pixelated, which is what a 1.35x downsample does to text). wallboardcheck's mount contract is rewritten to assert the gap, the non-coplanarity and that the assembly stays on the board: 18/18 PASS.
  5. **Background texture + pictures.** Azulejo 128 -> 256 px with a real zellij unit (central stars, corner quarter-stars, a colourway check per tile, grime, crazed glazes, one chipped tile) and TWO variants so the two wall flanks are not a mirrored repeat; frieze 512x64 -> 1024x128 with interlaced scrollwork, leaf lobes, palmettes and a bead-and-reel base course; floor 512 -> 1024 with per-paver tone, wear polish, chipped corners and hairline cracks; stone 256 -> 512 with per-block gradients, chisel marks, pitting and a damp/green base course. NEW: two knotted wall carpets hung on rods with finials and tassels either side of the tape board (`rugTexture()`: pile stripes, abrash, a hooked border vine, an eight-point medallion matching the azulejo/floor geometry, plus **baked drape shading matched to the geometric ripple** in the plane, which is what stops a hanging textile reading as a printed sticker). Pixel check `tools/rugcheck.mjs` (projects each rug, counts its hues): both present; vision review: "convincing folds and shading that give it depth", 7/10.
  6. **Tails behind, not down.** Measured baseline: the tail chain hung straight down along -Y and the two cats had OPPOSITE curl constants (-1.15 / +0.95), so A's tip sat 0.56 behind and 0.39 BELOW the hips while B's sat 0.26 in FRONT of the cat. Now one shared `TAIL_RAKE` (-PI/2 - 0.60 = raked back and ~34 degrees up out of the rump, riding 0.12 off the centreline so it clears the knight's cape sheet and the sultan's robe) plus a per-segment upward curl; the segments also overlap (0.15 capsule over 0.13 spacing) with a sphere at every joint, because the old 0.02 gaps read as a segmented stick ("several segmented pieces that do not smoothly connect"). Measured after: both tails **0.42-0.52 behind and 0.52-0.62 ABOVE the hips** in every sampled state (idle, both guards, every swing hold). Blades thickened (rapier r 0.006-0.013 -> 0.009-0.018, scimitar tube 0.024 -> 0.030) after a review called the rapier "almost invisible".
  - Verification: `tools/errcheck.mjs` 0 errors; `tools/rolecheck.mjs` ALL PASS (seated 29/32 samples under a leaning tape, exact one-sided attack ownership both directions); `tools/lanecheck.mjs` ALL PASS (proportional lane, 36.5% of frame width of visible slide); `tools/wallboardcheck.mjs` 18/18 PASS incl. the new mount contract; `tools/verify_ui.mjs` 21/21 (no overflow at 1280/768/390/320 with the new PRICE LANE panel); `tools/verify_colors.mjs` BUY green / SELL red intact; `tools/identitycheck.mjs` ALL PASS; `tools/verify_chart.mjs` ALL PASS incl. LIVE; `tools/rugcheck.mjs` both rugs present with their hues; `tools/bladeprobe.mjs` swing spans 26-30 frames/cut; `tools/probe.mjs` 30 s: 0 errors, 302 draw calls, 31370 tris, gapMin 0.93, 81.9 state-runs/min (the v18 tempo was 121-129 with ~10-frame swings - the fight is deliberately slower and legible now); `tools/poselab.mjs` + `tools/audit.mjs` + `tools/wallshot.mjs` are the new pose/frame labs.
- 2026-09-16 (**v18.1: board pulled off the wall**). User: "The chart is collide with the wall. Pull it a little closer" (screenshot showed the price digits clipped into the stone). Root cause: the v18 board was NOT proud of the wall - its back face (-6.99) sat 0.04 behind the wall face (-6.95) and the coping stone (front -6.87, the deepest protruding slice) cut straight through the board's back half at mid-height; the cleats were fully buried. Fix in arena.js: `BOARD_STANDOFF = 0.24` off a new `wallFaceZ` reference, so wall face -6.95 / coping -6.87 -> 0.24 shadow gap -> board back -6.71 (centre -6.62, front -6.53); board narrowed 8.2 -> 8.0 so the trim (half 4.05) clears the pier inner faces (4.10); frame trim deepened to a 0.26 shadowbox edge (screen recessed inside a proud frame); cleats lengthened to 0.29 to span the whole gap (0.02 in stone, 0.03 into the board back); the old radial contact-shadow halo (which painted the wall dark around the board and made the mount read RECESSED to the vision pass) replaced with a DIRECTIONAL cast shadow - a soft dark band below the board only, the physical signature of something mounted proud. skychart.js no longer hardcodes depth: the chart plane rides `userData.tapeBoard.z` (+0.09) from scene.js; WALL constants are now the no-board fallback only. tools/wallboardcheck.mjs grew 4 hard NO-COLLISION assertions read from the live scene: board back proud of coping front (gap >= 0.02), proud of wall face (>= 0.05), chart plane within 0.02 of board front (no z-fight), trim inside the blanked centre. Vision-vs-assertion lesson: eyes.py twice called the standoff "flush/sunk" from a tight crop while the measured pixel signature proved the mount (luminance dip to 50 just below the board edge, recovery to 108 on lit stone below the cast band); trust the deterministic probes, treat the vision verdict on subtle depth cues as noise. Verification: wallboardcheck 15/15, verify_chart (incl. live), rolecheck, identitycheck, verify_ui, mock-li... [truncated]
- 2026-09-16 (**v18: chart to the wall + role rule + identity swap**). Three user directives in one pass:
  1. **"take the chart out of sky and put it on the wall. Make wall blank so chart is visible"**: `src/skychart.js` rewritten from a screen-space panel (unprojected viewport rect at z=-24) to a WORLD-MOUNTED screen on the back wall: plane at z -6.72 inside a new walnut display board (8.2 x 3.17 at y 3.19, face z -6.81) that arena.js hangs on the wall centre, 0.09 in front of the board face. The board has frame trim, mounting cleats and a radial contact shadow on the stone so it reads as carpentry, not a decal. Canvas still sized ~1:1 to the PROJECTED board (fit to min(board, visible slice) x 0.92 on narrow viewports; fonts bumped one step after the vision review called the header text small). The wall centre is BLANKED: the azulejo dado + frieze are split into two flank segments (half-width 3.9 clear), the INNER piers + their torches moved out to x +-4.6. Stats expose the projected `screen` rect so checkers assert framing without THREE.
  2. **"If the pressure is sell, make the sell cat to attack and the buy cat to sit down and defend with his sword"**: director.js role rule. Pressure SIGN (smoothed pS) assigns roles every tempo tick: atkSide = P>=0 ? A : B, the other side defends (PARRY_BEAT/PARRY_HOP answers only; the REACTION table no longer counter-lunges). `_start` gate: offensive moves can only START on the pressure side (sanctioned exceptions: the post-lock winner follow-up, reason 'winner'). New SIT_GUARD posture (3.4 s, renewed by `_maintainGuard` while |pS|>=0.10 and gap<=2.6): crouch 0.30 seat on the rope, sword arm up in a closed high guard, off arm braced, tail wrapped; a seated cat can still be interrupted by his own parries (`_canMove` carve-out) and a seated guard counts as a guard for tryClash. Dead tape (|pS|<0.05) = probe-only (LUNGE/FEINT), no heavy slashes. HUD label 'HOLDS GUARD'. Verified by new `tools/rolecheck.mjs` (drives setPressure +/-0.85 with the feed stopped, reads the director's move log + 250 ms state samples): BUY tape -> A 9 attacks / B 0, B seated 22/32 samples; SELL tape -> B 7 attacks / A 0, A seated 19/32; both directions PASS.
  3. **"the moslem cat must be the buy and (green trace on dress) and the spanish one must be the sell (red clouthes)"**: identity swap. The BUILDERS swapped slots (Sultan = CAT A = BUY = right pole = green trim; Don Gato = CAT B = SELL = left pole = red cloth) and palette.js re-aligned the alias values (crimson* is red again, emerald* is green again, Sultan's eyes green, his gem emerald; Don Gato's gem ruby). Each cat kept his own costume (dishdashah stays on the Sultan, rapier/morion on Don Gato); sword arms re-homed to the camera side for the new facing. HUD plates updated (SULTAN BIGOTES on the BUY plate). Verified by new `tools/identitycheck.mjs`: catA name == SULTAN BIGOTES with 8 green-ish mats and 0 red-ish; catB == DON GATO with 36 red-ish and 0 green-ish; pixel cross-check: LEFT cat 4413 red px, RIGHT cat green trim px present.
  - Also: the 3D price banner moved from rope segment 8 to 5 - segment 8 hung it straight over the board (the first eyes.py review flagged "TBTC" text-on-text); skychart fit uses the arena board rect passed through scene.js (`board: arena.userData.tapeBoard`); verify_chart/moonslotcheck/verify_chart_mock rewritten for the wall contract (board ON the wall z/y band, live projected screen rect, extreme-pan board-over-moon = legal occlusion).
  - Verification: `tools/errcheck.mjs` 0 errors; `tools/verify_chart.mjs` ALL PASS (demo desktop/mobile 320/390 + LIVE: feed OPEN, 300 candles, price agrees); `tools/rolecheck.mjs` ALL PASS; `tools/identitycheck.mjs` ALL PASS; `tools/wallboardcheck.mjs` ALL PASS (board lum sd 72 vs wall 0, candles green+red+gold+text px present, band below board 0 azulejo px, flanks geometrically clear of the centre, board pans with the wall); `tools/verify_chart_mock.mjs` ALL PASS (incl. 390/320 fit); `tools/moonslotcheck.mjs` ALL VIEWPORTS PASS; `tools/verify_ui.mjs` 21/21; `tools/verify_colors.mjs` intact; `test_candles.cjs` ALL PASS; `test_providers.cjs` 56/56; `tools/probe.mjs`: 125.8 attacks/min, gapMin 0.83, 0 errors; eyes.py vision pass x2 (second pass: board clean, no z-fighting, no parapet clipping; header font bump applied).
- 2026-09-16 (**v17: fountain deleted**, user: "Delete the fountain. No need for replacement", one day after commissioning the v16 rebuild). Full tear-down, no replacement: arena.js fountain structure + marble/AO helpers + basin inlay + azulejo band + coins, ambience.js `buildFountainWater` (~245 lines), scene.js wiring, director.js coin-pop on pair stumble ("Fountain Fumble" renamed), vfx.js `coinPop()`, `arena.userData.coins` registry, and all fountain probe tools + screenshots. Verified: errcheck 0 errors; vision review confirms NO fountain in frame, nothing broken, floor medallion now the central feature (composition 7/10); former fountain region has 0 pool-blue px. Probe 117.8 attacks/min, gapMin 0.78, 253 draw calls (-39 vs v16). Deployed aa5232f (+010e429 tool cleanup), bundle sha 4157b707692d live == local, DEPLOY VERIFIED.
- 2026-09-16 (**v16: the fountain rebuilds into a working two-tier centerpiece**, user: "The fountain in the middle is so low quality. Make it beutiful and relevent to the project or delete it if you cant. Try that and if its not good, try a garden instead."). Old fountain = two flat cylinders (dry tub + dark disc); pixel metrics on a zoomed crop: meanSat 0.584, 562 4-bit colors = texture noise, not design. Rebuilt instead of deleted, and made thematically load-bearing: the water tints with the LIVE tape pressure (BUY green / SELL red, eased 0.9s tau) - the fountain joins the medallion, flag and HUD as a tape-driven element.
  - arena.js: stepped octagonal plinth, azulejo band (4 BIG eight-point stars with grout lines - fine patterns vanish at arena distance), wide lower basin with rolled marble lip, BUY/SELL split-star inlay in the basin floor (same convention as the duel medallion), pedestal, upper bowl, finial; stone-grain bump textures on all marble; 4 radial-gradient AO contact-shadow discs ground the tiers; coins lifted to the real basin floor.
  - ambience.js water (3 vision-review iterations): ballistic main jet from the finial + 4 spill arcs bowl->basin + foam churn at both landings + LIT pools (MeshStandardMaterial #7FA8C9, roughness 0.15, catches moonlight) in both tiers + ripple rings on both surfaces + moonlit motes. LESSON (cost 3 iterations): additive water planes/cones read as sci-fi glow or bury the scene in white mush (3/10); lit pools + particles is what reads (7-8/10). `scene.js` calls `amb.water.setPressure(director.pressure)` every frame.
  - Verification: vision review via poker3d-web `eyes.py` x6 + pixel classifiers (`tools/fountaincheck.mjs`, `tools/tintpixels.mjs`: BUY green px 892 vs SELL 500, tint PASS); in-scene rating 8/10 (BUY) / 7/10 (SELL) vs 4/10 baseline; fountain is the centered focal point (screen y 0.60-0.99, ~38% of frame height at default camera). Regression: verify_ui, verify_chart_mock, errcheck, steadiness all green; probe 119.8 attacks/min, gapMin 0.79, 292 draw calls (+23), 0 errors. Deployed commit 4895547, bundle sha 63b6336ac8fc live == local, DEPLOY VERIFIED. Garden fallback NOT needed.
- 2026-09-16 (**v15: the moon moves out of the chart's way**, user: "The btc moon is behind the btc chart. Put the moon on the side so the chart is more visible"). Root cause measured, not guessed: the moon was WORLD-fixed while the sky tape panel is SCREEN-fixed (upper-right, x 0.672-0.985). `tools/moonprobe2.mjs` projected the disc across the director's full pan range: screen cx swept 0.82 -> 0.17 as the camera followed the pair, so whenever the fight drifted left (mid goes to -7.3) the moon parked straight behind the panel; on a phone the 5.0-unit disc spanned ~77% of the frame width and covered the chart band at nearly every pan (and the narrow chart spans x 0.045-0.955 itself).
  - Fix: `scene.js` now owns the moon's screen slot every frame (the `MOON_SLOT` table), unprojecting the slot onto the moon's z plane with the SAME ray march the sky chart uses (`unproject -> normalize -> march`; the first attempt skipped `normalize()` and the unprojection exploded to y=102 - caught by the checker, not by eyeballing). The moon tracks 0.78-0.92 of camera travel (`par`), so it keeps real parallax but its leftover drift can never reach the panel band.
  - Three slots: wide (>=1024) cx 0.155/cy 0.170 at full size (left of the meter, opposite the chart); tablet (700-1023) 0.85x tucked to cx 0.130; phone (<700) 0.52x corner park at cx 0.130/cy 0.050 with slight frame bleed (a moon rising at the frame edge; the old 5.0-unit disc was comically huge on a 390px frame). `moonPulse` halo/scale breathing preserved; `arena.js` keeps only the first-frame position.
  - Verification: `tools/moonslotcheck.mjs` (9 pans x 3 viewports, asserts chart overlap/HUD/bleed with square-pixel y radius) ALL VIEWPORTS PASS; `tools/moonshot.mjs` + `tools/moonpixels.mjs` pixel evidence: the chart band contains ZERO moon-orange pixels in every shot while the moon slot holds 1-6k; `tools/moonframes.mjs` presence over 6 frames: wide [1034..4065] orange px, phone [506..607] - MOON VISIBLE every frame (dips = a cat legitimately walking in front). Regression: verify_chart, verify_ui, verify_chart_mock, verify_colors, errcheck all green; probe 113.9 attacks/min, gapMin 0.82, 269 draw calls. Deployed commit ad3050d, `deploycheck.mjs` bundle sha ba2e737724a1 live == local, DEPLOY VERIFIED.
- 2026-09-14: children completed; root goal-mode run died to a commandcode/deepseek-v4.1-flash provider outage (5 stale attempts) -> goal judge blocked the task falsely; re-verified deliverable, deployed to GitHub Pages (Netlify deploy-credits exhausted until 2026-09-23), completed root with evidence. Workspaces GC'd by `kanban complete` (deliverables preserved via kanban_complete attachments).
- 2026-09-14 (later): **v2 ambience upgrade** shipped to the same URL. New `src/skydome.js` (multi-stop gradient dome, 3 twinkling parallax star layers + Milky Way band, moon-crossing cloud wisps, shooting stars) and `src/ambience.js` (fireflies, animated fountain water, ember drift). Side-arch voids replaced with painted village vistas (glowing windows, hills, cypresses). BTC moon now breathes + flares on whale trades / blade clashes (moonPulse). Mouse-parallax camera. Fixed pre-existing duplicate-callout bug (spawnCallout was registered twice). Verified: bundle byte-identical local vs live (sha256 d9feb05dea551243), live headless pass 0 errors / 90 FPS, vision-QA rated background 8/10. Gotcha logged: fog + transparent renderOrder overrides hide anything below y~8 at z<-5; wisps/vistas must be tuned to the visible sky sliver.
- 2026-09-14 (v4): user reported the background "shaky". Diagnosed with a **temporal stability probe** (`stab.mjs`: 8 screenshots 90ms apart, mean |delta| per screen region between consecutive frames) instead of guessing. Baseline: sky 0.21 (frozen) but wall **20.0**, dado 15.8, floor 5.3 -> the background was changing almost as much as the fighting cats, while the camera itself was static. An amplified frame-difference map showed the character of the change was: solid silhouettes (cats, expected), particle dots (ambience) and **fine speckle along static patterns** (arch rings, star panels, floor grid, medallion rim) plus a uniform region glow = **lighting flicker**. Fixes, all measured: (1) torch lights strobed 1-4 Hz at +-40% of intensity and were pumping the whole wall => now slow shallow breathing (13 +-1.5, 0.9-3.1 rad/s); (2) **shadow map disabled again** - it cost ~40% of the frame budget and its per-frame re-render speckled the floor (the only receiver) => floor 5.3 -> 0.76, FPS 45 -> 93; (3) new `src/tex.js` central texture factory: mipmaps + `anisotropy = min(8, maxAniso)` on EVERY canvas texture, handed the renderer's max via `setMaxAnisotropy()` before any texture is built; (4) softened the hard-edged fine patterns that crawled (floor grout 3px solid -> 2.5px @0.66 alpha, medallion/azulejo star edges blurred 0.45/0.35px via ctx.filter); (5) camera: the fighter-follow pan is now low-passed (a stumble teleports a cat 0.5 units and used to snap the whole frame with it), mouse parallax got a 0.07 dead zone + slower easing, idle drift halved; (6) rim lights' throw shortened (44/9 -> 34/7) so swinging with the cats cannot pump the back wall; (7) fewer/slower/dimmer ambience particles (fireflies 70 -> 44, embers 46 -> 28, slower blink/rise) because at close range their blinking read as instability. Result: wall 20.0 -> **6.19** (-69%), floor 0.76, sky 0.03, FPS 93. Live verified: bundle byte-identical (sha256 2d7d8870e8c47ff4), 0 page errors; only the sandbox's Binance geo-block logs WebSocket 451s and the page correctly falls back to SIMULATION.
- 2026-09-15 (**v5 combat + art pass**, user: "the rope movement is a lot and too shaky", "the circles on the background are laggy and ugly, you can delete it", "cats move too little, fight more and faster and better", "you can use textures for 3d surfaces"). Four workstreams, each measured:
  1. **Arch hoops deleted** (user picked them out of four candidate "circles"): the 5 giant torus rings + ~90 voussoir blocks + 4 vista planes (~110 draw calls) are gone. The back wall is a clean open parapet (piers, coping, merlons, azulejo dado, ataurique frieze), torches moved onto the pier tops, BTC moon free in the sky. Also fixed a v5-introduced bug that made the wall render near-white (a texture factory call passed `#ffffff` as the material colour), and turned the 6 tiled floor medallions into ONE decal under the rope.
  2. **Rope**: damping 0.985 -> 0.93, gravity -5.2, sway clamp 0.35 -> 0.12, every impulse scaled to 0.16 (`DIM.impulseScale`), and the mesh now follows a low-passed + spatially smoothed copy of the physics points (cats + flag ride the smoothed rope too), so 120 Hz solver noise never reaches the pixels. The tube is rewritten in place (no per-frame `TubeGeometry` + `dispose`). Measured on the rendered points: mean speed 0.43 -> 0.26, mean |accel| 12.6-19.7 -> 6.6, y-travel 1.36 -> 0.43, max y-travel 1.81 -> 0.68.
  3. **Fight**: pair locomotion closes the gap on a circle -> engage -> break cycle (mean gap 2.6 -> 1.07, under 1.6 in 92% of frames vs 5%), a tempo clock forces a move every ~0.45 s in a brawl (idle 84% -> 38%), attacks/min 8-14 -> ~128, clashes 0-1 -> ~6 per 30 s, new HIT reel / BLADE_LOCK / RUSH states, A/B attack directions fixed (A used to lunge into its own pole), feet planted on the rope surface each frame (a paw used to sink into the hemp), and a hard 0.68-unit floor on the rendered separation so bodies never interpenetrate. Locks are spaced 2.2 s apart with a forced break after each (the live tape had the cats welded together 43% of frames before that).
  4. **Textures for 3D surfaces**: `src/tex.js` gained fur (cream patches / irregular tabby), cloth weave, brushed metal, leather, stone ashlar and braided hemp, each with bump (and a roughness map for the rope). Rope = 3 longitudinal strands + grooves + fibers, plus a 21% twisted elliptical cross-section and radial bulges so the strand lay reads in the silhouette at ~35 px thickness.
- Lesson learned (logged in skill `threejs-3d-game/references/art-verification-loop.md`): a vision model is a poor judge of small procedural surfaces and can hallucinate outright (it called an isolated rope render "a financial trading interface"), so verify with measurements - pixel-diff the object with/without its map, read the tube's per-ring radius spread, and design texture features for the ACTUAL on-screen pixel budget (the crushed axis mip-averages anything thinner than ~1/10 of it).
- Verification: `tools/errcheck.mjs` 0 boot errors; `tools/probe.mjs` 30 s telemetry (rope + fight + perf) with 0 errors, 128 attacks/min, gap min 0.68, 243 draw calls (was 271-331), geometries 252 (was 365), frame 18.5 ms; `tools/ropelab.mjs` rope geometry + texture-vs-flat detail (+8); `tools/ropegone.mjs` frozen-frame A/B proves the rope pixels; `tools/livecheck.mjs` on the DEPLOYED URL: bundle sha256 equal local, 0 errors, live Binance feed (76,953.45 / OPEN), 19 moves + 3 clashes + 3 blade locks + 12 hits in 12 s, gap min 0.68, green voussoir pixels ~0 (hoops gone). Deploys: commits 3642f04 -> 3b1fb2f -> 0373c01 pushed to main, GitHub Pages serves the new bundle.
- 2026-09-15 (**v6 rope thickness + costumes**, user: "the rope is too thick now", "put the dishdashah on the moslem cat and a spanish knight clothes on the other cat").
  - **Rope back down**: `DIM.ropeRadius` 0.105 -> 0.065 (world diameter 0.128-0.152 units with the strand lay, was 0.20-0.25), on-screen thickness 35-43 px -> 20-30 px. Twist bulge 0.12 -> 0.09 and lay 0.20 -> 0.16 so the strands do not inflate the outline, and the painted underside shading was softened (it read as a hard dark line along the bottom of a thin rope). Strand read kept: texture detail 32 vs 28.6 untextured.
  - **Sultan Bigotes (SELL) now wears a DISHDASHAH**: long ankle-length robe (body on the spine so it leans with the chest, flared skirt on the hips down to the paws) with an animated hem, starched pleated collar with an emerald band, front placket with three silver buttons, wide upper and forearm sleeves with emerald cuffs, and the silver belt worn over the robe with the crescent-moon pin. Removed the kaftan, sash, ribbons, breeches and babouches. Turban, silver vambrace and scimitar kept.
  - **Don Gato (BUY) now wears Spanish knight clothes**: starched white golilla ruff (two pleat rings, outer radius 0.305), gold gorget, a morion with a wider brim and a tall comb ridge, gold cuisses/tassets over the thighs, a leather baldric across the cuirass, crimson SLASHED doublet puffed sleeve caps with gold arm-bands, and thigh-high brown leather boots with folded cuffs, buckles and heel wedges (the old gold shin plates are gone). Cape, breastplate, pauldrons and rapier kept.
  - New procedural textures: `pleatTexture` (starched linen pleats for the ruff/collar) and `slashTexture` (slitted doublet with the liner showing), plus a linen weave for the robe.
  - Verification: 0 errors, 17.7 ms frame, 263 draw calls, 29.5 k tris; rope mean |accel| 3.4, y-travel 0.43; 126 attacks/min, 7 clashes + 14 blade locks per 30 s, gap min 0.68 (never interpenetrates). Vision review of both outfits: "robe long, covering the legs down to the paws, no clipping or leftovers", ruff + morion ridge + leather boots all confirmed visible, "nothing clearly broken or ugly".
- 2026-09-15 (**v7 buy/sell color flip**, user: "the green cat are for sell and red one for buy. flip that"). Standard financial convention now holds: **BUY = GREEN, SELL = RED** everywhere.
  - **Token-level change** (`src/palette.js`): Cat A (Don Gato, BUY, right pole) keeps his gold armour neutrals; all his cloth (cape, plume, tabard, slashed doublet, scabbard straps) moved crimson -> BUY green `#1B7A4B/#0E4A2C`, pommel gem ruby -> emerald. Cat B (Sultan, SELL, left pole) keeps silver/white; all his trim (turban band, placket, cuffs, hem, collar band, scimitar grip) moved emerald -> SELL red `#A31621/#C0392B/#7A0C14`, eyes jade -> red `#FF8A80`, gem emerald -> ruby. Legacy `crimson*`/`emerald*`/`gem*` alias names kept pointing at the SAME material slots so un-migrated references still resolve to the new hues; new `SIDE` tokens (BUY/SELL + bright/deep) are the canonical side hues. Cat identity is by POSITION (A right = BUY), never by color, so director/physics logic is untouched.
  - **Consumers flipped**: HUD (`index.html`: new `--buy/--sell/--buy-bright/--sell-bright/--sell-dim` tokens; meter gradients, meter labels, cat tags, callout text/border/bg), rim lights (`scene.js`: A green, B red), crowd pack tints (`crowd.js`: green pack right, red pack left), combat VFX (`vfx.js`: A-side sparks + lunge sparks green, B-side ghost/ghostTile red), floor medallion (`arena.js`: green half points right/BUY, red half points left/SELL). Already-correct and left alone: BUY/SELL pole pennants, price flag up/down fills, HUD up/down classes.
  - **Deterministic verification** (new `tools/verify_colors.mjs` + scene `freeze` debug hook): pins the cats at +/-1.1, kills the demo tape, asserts computed DOM styles and saves comparable before/after screenshots. BEFORE (real git HEAD bundle): BUY tag/meter/callouts gold, SELL jade. AFTER: BUY green `rgb(53,208,127)`, SELL red `rgb(239,83,80)`, 0 console errors both. Vision QA on the frozen frames: right cat green cape + gold armour, left cat red-trimmed white robe + silver, medallion red-left/green-right, pennants red-left/green-right. Pixel-classification tools (`colorflip_check.mjs`, `colorflip_bands.mjs`) kept in tools/ but their whole-frame verdicts are noisy (crowd packs + azulejo tiles share the hues); the frozen-frame vision pass + DOM asserts are the authoritative check.
  - `tools/probe.mjs` 30 s: 0 errors, 102 FPS, 127.8 attacks/min, 5 clashes + 14 blade locks, gap min 0.68, 263 draw calls - identical to the v6 baseline, the flip is purely cosmetic at the token layer.
- 2026-09-15 (**v8 "Midnight Arena" UI overhaul**, kanban `t_addd6df0` — full visual/UX polish pass on top of v7 + v7.5).
  - **Design system**: self-hosted premium fonts (`fonts/`, fontsource woff2 via jsdelivr — Google CDN blocked on this line): Unbounded 500/700/900 for display (brand, hero price, fighter names), Space Grotesk 400/500/700 for data. Double-bezel plates everywhere (outer shell: hairline border + plate bg + 7px pad + 22px radius; inner core: panel bg + inset top highlight, concentric radius), custom easing tokens (`--ease-out` cubic-bezier(0.22,1,0.36,1), `--ease-snap` with overshoot), static SVG film-grain + radial vignette overlay (fixed, pointer-events:none, no repaint).
  - **Hero hierarchy**: price is now the dominant element (Unbounded 700 30px gold with glow), inside a bezel HUD. Green/red tick flashes on the tape direction (`.tick-up/.tick-down`, 600 ms decay, transition-colors only).
  - **Pressure meter**: tug-of-war blade in a bezel track (inset shadow), fills with glowing diamond leading edges, live `NN%` labels per side, and a duel-state pill straddling the TOP edge (EN GARDE / CROSSED SWORDS / CLASH! / BLADE LOCK) so it never covers the fills.
  - **Fighter plates** (left SULTAN BIGOTES = SELL red, right DON GATO = BUY green): crest + name (display font) + role + momentum bar (|P| driven) + live state text. Wired to the REAL fight state via an 8 Hz poll of `__duelDebug`: `data-act` reactions — lunge/riposte/rush = plate lunges toward center, blade lock/clash = core shake, hit/stumble = brightness-pop — all transform/opacity only.
  - **Callouts**: two-line glass pills (head: ▲/▼ + $notional, sub: qty @ price), whale tier gets a bigger pill + shockwave ring; edge-clamped so they never clip (measured getBoundingClientRect reposition).
  - **Micro-interactions**: staggered entrance (blur+fade+rise, 0.05-0.34s delays, one-shot), chart panel hover-to-expand, status/chart chips kept. Reduced-motion media query disables entrance/pulses/fills transitions.
  - **Contract fixes**: fixed elements center via the `translate` property (NOT transform) so the `ui-in` entrance animation composes without clobbering centering; `#chart-panel` keeps explicit height (canvas sizing contract); chart markup got a `#chart-inner` bezel core (chart.js API untouched).
  - **feed.js**: pure demo mode no longer fires the REST kline seed fetch (demo tape builds its own history) -> sandboxed/offline boots run with ZERO console errors (errcheck now fully clean).
  - **Verification**: new `tools/verify_ui.mjs` 21/21 PASS (fonts loaded, entrance ran+finished, all plates rendered, tick flashes up/down+clear, meter widths+pct labels exact, momentum bars, live combat reactions HIT/RIPOSTE/RUSH/PARRY, duel pill act-state, no overflow + all panels inside viewport at 1280/768/390/320, reduced-motion honored, avg frame 10.5 ms headless, zero console errors); `tools/verify_colors.mjs` re-run: BUY green / SELL red intact incl. new role labels; `tools/verify_chart_mock.mjs` 12/12 PASS (chart works in the new bezel; panel fits 390/320); CandleStore unit tests all pass; `tools/probe.mjs` 30 s: 129.8 attacks/min, gap min 0.68, 261 draw calls — scene untouched (UI-layer only). `tools/shot_ui.mjs` + `tools/shot_callout.mjs` capture before/after + whale-callout evidence.
  - Vision QA loop on the shots: fixed sword icons reading as "X" (stroked -> filled silhouettes), meter pill occluding the fills (moved to top edge), dim state text (+contrast), callout edge clipping (clamped). Note: the red price flag in the bottom-left of screenshots is the scene's 3D PriceFlag (pre-existing v1 behavior, out of UI scope).
- 2026-09-15 (**v7.5 embedded live chart**, kanban `t_189fb722`, feed contract from `t_6f447e8c`).
  - **feed.js**: `btcusdt@kline_1s` added to the SAME socket (per contract — no second connection; endpoint #3 stays trade-only and still gets candles from prints). New `CandleStore` (300 x 1s ring buffer, rev-gated consumers): REST seed `api.binance.com/api/v3/klines?...interval=1s&limit=300` fetched once per `start()` (flat weight 2, ACAO *; failure harmless), `@kline_1s` ticks upsert by candle time (interim + final), every trade print folds into the current second (demo mode included). Public `feed.candles()`. Unit-tested in `tools/test_candles.cjs` (21/21: slots, upserts, seed overlap, trim, garbage input).
  - **src/chart.js + chart.css**: hand-rolled zero-dep canvas candlestick panel embedded bottom-center of the arena (not a separate page). ~4 px/candle adaptive window (line fallback below 2.2 px), nice-step grid + right price scale, 30 s time ticks, dashed last-price line + right tag, whale/side dots ($50k+ callout trades), status chip mirroring the feed lifecycle (LIVE / RECONNECTING / SIM TAPE / STALE FEED after 8 s of frozen data with the socket still open). Redraws gated by the feed's 250 ms tick + rev/price change (quiet tape costs nothing). Colors read the page CSS tokens (`--up/--down/--buy/--sell/--panel/--edge`) every draw, so the v7 flip and future theme work propagate automatically.
  - **Wiring**: `index.html` (+stylesheet link, +panel markup), `main.js` (init; `handleState` -> `chart.update(feed.candles(), s)` ~4 Hz; `setStatus` -> chip; `spawnCallout` -> dots; single callout funnel so no dup dots).
  - Verification: `tools/verify_chart.mjs` (demo mode + 390/320 px fit, pixel-paint asserts, growth-over-time) and `tools/verify_chart_mock.mjs` — because the sandbox's egress started refusing Binance mid-task (451/ECONNREFUSED on REST+WS; parent had verified the same endpoints live 45 min earlier; netprobe shows example/github fine), live-mode was proven with Playwright `routeWebSocket` + `route` feeding real-shaped Binance frames through the production feed/store/chart path: 12/12 PASS incl. seed ingest (300 candles), LIVE -> STALE -> LIVE chip cycle, zero console errors. Vision QA of the panel: 9/10. `tools/errcheck.mjs` clean; demo fallback verified (offline -> SIM TAPE chip, chart keeps building from prints).
- 2026-09-15 (**v9: the live feed was not actually live on the user's line**: root verification task `t_617b37cf`). Root-side verification of the v7/v7.5/v8 children found the whole point of the feature missing on the network that matters: from the raw Iranian IP (cloudflare trace `loc=IR`, 178.131.154.211) every Binance host answers **HTTP 451 / non-101**, and Coinbase (ECONNREFUSED), Kraken, Gemini (451), Bitstamp and Bitfinex (403) are blocked too, while Bybit answers REST 200 **with `access-control-allow-origin` echoing the page origin** and streams ~12 trades/s over WS. So `?mode=auto` spent its entire 2-attempt fallback budget on two dead Binance mirrors and dropped to the synthetic SIM tape. The earlier "live verified" claims were all made through the VPN underlay, not the raw line. Fixed by making the feed multi-provider.
  - **Provider abstraction** (`feed.js` v1.1.0): each endpoint carries `kind: 'binance' | 'bybit'` and `_connect` selects the frame parser from it, so exactly ONE socket is still open. Bybit v5 needs an explicit `{op:'subscribe',args:['publicTrade.BTCUSDT','tickers.BTCUSDT']}` after open plus an app-level `{"op":"ping"}` every 20 s (browsers auto-answer protocol pings, Bybit still drops the socket); both added. `_handleBybit` normalizes topic+data frames onto the same internal trade/price shape: `publicTrade.S === 'Buy'` is the same aggressor convention as Binance `m === false`, and `tickers.price24hPcnt` is a FRACTION so it is scaled to the percent unit the rest of the page already uses.
  - **The fix is the endpoint ORDER, not just the extra provider**: Bybit sits SECOND, directly after binance.vision. All Binance mirrors fail together in a blocked region, so a mirrors-first rotation burns the whole `demoFallbackAfter` budget before reaching a host that answers. Measured: on the blocked line binance.vision fails in ~2.3 s, one backoff, Bybit is live ~1 s later (~4.5 s total); on an open line binance.vision wins attempt one and Bybit is never touched.
  - **Handshake watchdog**: a black-holed host can leave a socket half-open forever and stall the rotation, so every attempt is capped at `connectTimeoutMs` (8 s) and the resulting close drives the normal rotation path (one failure code path).
  - **Seed runs on the first successful open** (`_seedProvider`): a geo-blocked provider no longer gets a wasted cross-origin request, and history comes from whoever actually answered. Bybit's finest kline is 1 m, so its history is seeded from `recent-trade` prints instead (that endpoint caps at 60 rows regardless of `limit`, i.e. ~60 s of same-venue 1s history; the chart scales candles to the panel width, so the panel is full-width from the first paint).
  - **`CandleStore.seedHistory`**: the old `seedRows` contract (rows at/below the newest live slot are skipped, "live wins") is right for a pre-connect seed but throws the ENTIRE history away when the REST response loses the race to the socket, which is the normal case. `seedHistory` prepends history older than the first live slot, appends rows newer than the live tail, and leaves overlapping seconds to the live candle. `seedRows` is untouched so the existing unit test still pins its old behaviour.
  - **Thin-venue continuity**: Bybit spot BTCUSDT prints ~1 trade/s (12/s peak), which would punch holes in a 1-second chart, so every ticker push also refreshes the CURRENT second with the exchange's real last traded price. Order-flow pressure is deliberately NOT fed from that mark: it stays pure aggressor-side trade flow.
  - **HUD tells the truth**: the status badge renders `LIVE · BINANCE` / `LIVE · BYBIT` (socket jargon `open` is gone), so a provider switch is visible instead of silent.
  - **The chart header stopped lying**: `index.html` hardcoded `BINANCE SPOT`, which became false the moment the feed could run on Bybit. The venue text is now driven from the feed's provider (`#chart-venue`, neutral `SPOT MARKET` until the first status arrives), so the header, the status badge and the data always agree. Both are asserted by `verify_live_bybit.mjs` and `deploycheck.mjs`.
  - **Known cosmetic gap (pre-existing, NOT a regression)**: the in-world 3D price banner hangs from rope segment 8 by design, so at ~1.6 aspect its swallow-tail clips the left frame edge (the leading digit of the price is cut). Left alone deliberately: the hanging point is from the v1 design spec, its position is driven by the rope physics frame to frame, and moving it would either plant it over the duel (centre segments) or change a composition the user already approved. It is visible in every historical screenshot and improves on wider aspects. Candidate follow-up if the user wants it: hang it at segment 10 and/or scale the banner to 1.3 units.
  - **Verified on the real network, nothing mocked**: new `tools/verify_live_bybit.mjs` 22/22 across two scenarios: A) `?mode=auto` ends LIVE with the displayed price agreeing with an independent REST ticker for whichever provider answered (Binance: dev 0.012%, 300-candle seed, 388 trades/s; Bybit: dev 0.000%); B) a feed built with ONLY the Bybit endpoint goes live and grows a 1s series from real prints (dev 0.000%), so the fallback is proven deterministically whether or not Binance happens to be reachable. New `tools/test_providers.cjs` 56/56 (frame parsing, aggressor mapping, fraction->percent, rotation, subscribe frame, keepalive, both seeds, backfill, garbage input). No regressions: `test_candles.cjs` ALL PASS, `verify_colors.mjs` BUY green rgb(53,208,127) / SELL red rgb(255,138,128) with 0 console errors, `verify_ui.mjs` 21/21 (incl. no overflow at 1280/768/390/320 with the longer status text), `verify_chart_mock.mjs` 12/12, `errcheck.mjs` clean, `probe.mjs` 261 draw calls / gap 0.68 (scene untouched: this was a feed-layer change).
  - `tools/deploycheck.mjs` now hashes EVERY deployed file (`index.html`, `feed.js`, `bundle.js`, `chart.css`) instead of only the bundle: `feed.js` is a separate script tag, so a bundle-only hash check would happily pass a stale feed, and it now asserts the DEPLOYED page is genuinely live (provider named + price cross-checked against that provider's REST ticker).

- 2026-09-15 (**v11: frame budget + head contact + meter header**, user: "the rope duel is getting fps drop midfight. fix that. also the cats are too close and their head constantly collide to each other", then "look at the text location on top"). Three separate problems, each measured before and after rather than eyeballed. New instruments: `tools/fightprof.mjs`, `tools/screen_gap.mjs`, `tools/govtest.mjs`, `tools/restest.mjs`, `tools/liveload.mjs`, `tools/triple_probe.mjs`, `tools/headdiag.mjs`, `tools/shot_meter.mjs`, `tools/meter_ab.mjs`.
  1. **FPS: the scene is GPU-bound, not JS-bound, and nothing adapted.** Measured with `EXT_disjoint_timer_query_webgl2` + per-subsystem wrappers: JS (rope, director, both cats, vfx, crowd, render submit) totals **0.4 ms/frame**; the WebGL pass is **7 ms at ratio 1 vs 23 ms at ratio 2**. So the old `pixelRatio = min(dpr, 2)` cap was spending four times the pixels of a 1x display before a single cat was drawn. Fixes: (a) base cap **2.0 -> 1.5**; (b) a **self-verifying adaptive quality governor** (`?res=` pins, `?gov=off` disables): median frame time over 16-frame windows, steps the ratio 1/0.85/0.72/0.6/0.5, and if a step down buys no frames (measured: under real GPU saturation 1920x1200 -> 640x400 buys only ~16%, so the cost is contention, not fill) it reverts and stops meddling; (c) removed the fight-path per-frame churn: `rope.updateVisual()` no longer calls `computeBoundingSphere()` on 1469 vertices nobody reads (mesh is `frustumCulled = false`), no more `Vector3`/`THREE.Color`/pose-object allocation per frame (scratch vectors, cached tints, shared `IDLE_TEMPLATE`), cape + dish buffers marked `DynamicDrawUsage`. Measured after: **idle 73.6 / active 70.9 / brawl 69.8 fps** (the brawl bucket is no longer the slow one), 261 draw calls, 0 errors.
  2. **Heads: they really were colliding.** Baseline telemetry: head centres **0.02** units apart and nose tips **0.01** apart, i.e. overlapping on **59% of brawl frames** (the cats' heads are oversized by spec, reaching ~0.30 past the pivot, so the old 0.68 body floor could never be enough). The first fix (constrain the muzzle tips) failed in a way that took three measurements to understand: in RECOVER, the windmill every move ends with, the two muzzles point at each other and the tips are CROSSED, so pushing the bodies apart drove them *through* each other (0.231 -> 0.144 -> 0.128 -> 0.100 -> 0.077 over four passes), and rowing back left 34% of frames overlapping. Final design: ONE rotation-invariant constraint on the head PIVOTS (everything on a head lies within 0.33 of the pivot), pushed along the body order, iterated with forced matrix updates. `DIM.minHeadCentre = 0.80` (the geometric no-interpenetration distance is 0.66; 0.80 is chosen so the gap is *visible*, measured in pixels with `screen_gap.mjs`). Director retuned to match: engage gap 1.05-1.40 -> 1.45-1.85, break/circle raised, pair minSep 0.95 -> 1.05, lunge reach clamp `gap - 0.95` -> `gap - 1.15`. Results: head centres min **0.80** (was 0.02), frames with head overlap **0%** (was 59%), mean pair gap **1.02 -> 1.52**, on-screen head clearance **13-36 px, never negative** (was negative on 27% of frames), and the fight is unchanged in tempo: **127.8 attacks/min** (was 128), 6 clashes + 14 blade locks per 30 s, idle 41%, 0 errors.
  3. **Meter: the state text was sitting on the bar's edge and half clipped.** Measured: the badge was a child of `#meter-track { overflow: hidden }` with a `translate(-50%, -50%)` on the track's top seam, so **10.8 of its 21.5 px height lay outside the track** and the tug-of-war stem ran through the text; the HUD above leaves only **2.5 px** of room, so it could not float clear either. A placement A/B (`meter_ab.mjs`) confirmed it, and the fix is a real header row on the meter panel (matching the chart panel's title+chip pattern): left label `PRESSURE - 30s WINDOW`, right the duel-state chip, track below. The percentage chips got a soft plate so a full-deflection fill cannot wash out the number. Verified `verify_ui.mjs` 21/21 (no overflow at 1280/768/390/320 with the taller panel), `verify_colors.mjs` BUY green / SELL red intact, `verify_chart_mock.mjs` 12/12, `test_candles.cjs` ALL PASS, `test_providers.cjs` 56/56, `errcheck.mjs` clean.
  - Bash note for future sessions: a stray headless Chrome from an interrupted run silently shared the GPU and made every baseline look 3x slower (24 fps vs 73 fps). Run `tasklist | grep -i chrome` before trusting a perf number.

- 2026-09-15 (**v12: the tape moves into the sky**, user: "i think the chart is better to be embeded in the sky design and visible there"). The candle chart stopped being a DOM panel pinned to the bottom of the screen and became an object in the duel's night sky: one wide light panel floating behind the arena, drawn on its own 2D canvas and mapped onto a single plane (`src/skychart.js`). `src/chart.js` and `chart.css` are gone; the panel markup is gone from `index.html`; `main.js` now hands the same candle snapshots to `scene.chart`.
  - **Placement is measured, not eyeballed.** The obvious spot (a centred band above the cats) does not exist: the pressure meter ends at 0.22 of the frame height and the cats' HATS reach 0.335-0.384 (a head pivot projects at ~0.42, and the hat sits ~0.25 world units above it, which is ~0.08 of the frame at the cats' distance), leaving ~0.11 of the frame - a 998x100 px sliver. Rendered at 7:1 the review called it "a UI widget pasted on top" and it tangled with the right cat's feathered hat. The panel therefore sits in the upper-right quadrant (below the status chip, right of the meter, clear of the right fighter plate and of both hats), shaped ~2.4:1 so the candles and labels are actually readable.
  - **Layout is in screen space, placement is in the world.** A fixed world-size panel is illegible on a phone and collides on a wide monitor, so the panel is defined as a viewport rectangle and unprojected onto its own z plane (28 units out): y 0.104-0.348 of the frame on desktop, 0.175-0.30 on narrow screens. The canvas is sized so its pixels land ~1:1 on screen (`scale 0.59` at 1280x800), which is what keeps 12-14 px labels legible instead of smeared. Two ordering traps, both measured: the unprojection at boot read an identity view (the camera's world matrix is only built by the renderer) and the panel landed at 0.35-0.53 of the frame; and the resize listener ran BEFORE the scene's own handler, so it laid out with the old window shape (at 390px wide the panel sat at NDC x 1.03-2.93, entirely off-screen). Fixed by syncing the camera aspect inside the layout, deferring the resize layout by one frame, and re-checking the viewport on the feed tick (an embedded pane can resize without a window event).
  - **Three treatments** (`?sky=slab|aurora|stars`), default `slab`: a feathered backlit pane with gold rules and corner ticks (no hard border - a 1px rectangle over stars reads as a sticker), candles at 7.5 screen px with wicks (4 px blurred into a stepped line), a halo along the close path so the data emits light like the moon does, and the price scale + tag in the right gutter. `aurora` drops the frame for light columns and a ribbon; `stars` is a constellation of closes with the last price as a bright star, the most sky-native of the three.
  - **Details that came out of review:** the venue name is printed once (top-right status chip) instead of twice in one corner; the panel hangs at a slight tilt (yaw 0.062 rad) because a perfectly axis-aligned, crisply sharp rectangle is the classic tell of a screen overlay; the price tag clears the right edge; time ticks every 15 s because the window is ~40 s wide.
  - **Cost:** +2 draw calls (panel + glow plane), canvas redraw throttled to 2.5 Hz and gated on the feed's rev/price changes. Measured `fightprof`: idle 79.3 / active 81.4 / brawl 78.7 fps, JS 0.34 ms, GPU pass 6.7 ms, 267 draw calls, 0 errors - the panel is free at the margin.
  - **Verification:** new `tools/verify_chart.mjs` (rewritten for the sky panel: texture carries ink, the tape grows and repaints, the chip mirrors the feed, the panel is inside the camera frame, it sits behind the duel and above the wall, and its bottom edge clears the top of every hat that shares its columns) and `tools/shot_sky.mjs` (renders each treatment, full frame + crop). `tools/verify_ui.mjs` 21/21, `tools/verify_colors.mjs` intact, `tools/verify_chart_mock.mjs` ALL PASS (feed lifecycle + 390/320 framing via the sky panel), `tools/test_candles.cjs` ALL PASS, `tools/test_providers.cjs` 56/56, `tools/errcheck.mjs` clean, `tools/deploycheck.mjs` now hashes index.html + feed.js + bundle.js and asserts the sky tape paints.

- 2026-09-16 (**v14: technical fight craft**, user: "Make the rope duel smoother and more technical in battle moves"). Two goals, both measured: SMOOTHER (no constant-speed robot moves) and MORE TECHNICAL (moves that answer each other like real fencing).
  1. **Technique grammar (director.js):** the defensive reply used to be a blind coin flip (`PARRY_HOP` or `LUNGE` at random, 0.45 chance). A new `REACTION` table makes the answer MATCH the attack: charge -> sidestep, point/thrust -> beat, big cut -> beat, feint -> beat with raised 0.75 odds (the bait pays). New moves: **THRUST** (A, the fencing stop-thrust, 0.42 s), **FEINT** (A, half-lunge bait), **PARRY_BEAT** (B, crisp timing beat). New `COMBO` chains turn swings into phrases: LUNGE->THRUST->SLASH_UP, FEINT->THRUST, PARRY_BEAT->RIPOSTE, PARRY_HOP->RIPOSTE; chained moves skip the side breather (`{chain:true}`), depth caps at 2, `stats.combos` counted 9 per 34 s run. Lock-winner follow-up rotated 3-ways instead of always RUSH/SLASH_SPIN.
  2. **Eased choreography (cats.js):** every strike phase now accelerates in and settles out (`smooth`/`easeOut` helpers) instead of feeding linear fractions into the poses - rush, lunge (slow coil -> fast explode), slashUp, parryHop, slashSpin (windup whips up), riposte, clash knockback (shove then friction), hit reel (impulse then decay). Per-move RECOVER windmill lengths (RUSH 0.30 s vs BEAT 0.08 s) instead of one flat 0.16.
  3. **Smooth pair locomotion (director.js):** gap closing was CONSTANT-speed and stopped dead at the target (a march + halt every phase). Now exponential approach (fraction of remaining distance, rate-capped 2.6/1.8 1/s) that glides and settles. The lateral "hunt" weave is low-passed (dt*2.4) and fades by distance, so no sideways snap when the phase flips.
  4. **Blade trails (scene.js):** pooled 22-point ring buffer per cat, side-tinted (BUY green / SELL red), additive; emitted live from the real blade tip during swing windows (20-75% of LUNGE/RUSH/SLASH/RIPOSTE/THRUST) plus on onSlash/onThrust/onBeat hits. +2 draw calls total (265 live).
  5. **A real bug caught:** `_start` crashed with `Cannot read properties of undefined (reading 'cool')` - the new REACTION lookup returned the whole `{A,B}` object instead of indexing by the replying side. Fixed with `table[foe]`; `_start` now refuses unknown move names into `_badMoves` (diagnostic tools `tools/badmove.mjs`, `tools/errtrace.mjs` kept).
  - Verification: `tools/errcheck.mjs` clean; `tools/badmove.mjs` 34 s: 0 errors, 0 bad moves; `tools/probe.mjs` 121.9 attacks/min, gapMin 0.78, THRUST/FEINT/PARRY_BEAT all firing, 263 draw calls; `tools/steadiness.mjs` 34 s vs v13: 2 s attack-burst CV 0.53 -> 0.47, camera push-in reversals 0.108/s -> 0, push sd 0.044 (v13 0.043), heat sd 0.158 -> 0.152, pace 1.91 -> 1.97-2.05 attacks/s, combos 9/run; `verify_ui.mjs` 21/21; `verify_colors.mjs` BUY green / SELL red intact; `test_candles.cjs` + `test_providers.cjs` (56/56) ALL PASS; `verify_chart.mjs` + `verify_chart_mock.mjs` ALL PASS. Deployed commit 12c6d8b, `deploycheck.mjs` DEPLOY VERIFIED (bundle sha 28eda4e2a05b live == local), `livecheck.mjs` on the deployed URL: 0 errors, LIVE · BINANCE, all new states live on the real tape.

- 2026-09-15 (**v13: a steady fight rhythm**, user: "also the fight momentom is flakey. i want it to be a little more steady"). The fight's energy was a random walk wearing a duel costume. Measured first (`tools/steadiness.mjs`, new): attacks arrived with an interval CV of **1.16** (an sd larger than the mean), 2-second windows swung between **0 and 6** attacks, the crowd's heat sawtoothed from **0 to 0.83**, and the camera reversed its push-in direction **0.25 times a second**. Five causes, all of them "a random number where a beat belonged", each fixed at the source:
  - **The decisions read the raw tape.** `pressure` arrives at ~4 Hz and is noisy, and aggression, tempo and gap targets all keyed off the instantaneous value, so the fight changed character frame to frame. Decisions now read `pS`, a 1.2 s exponential average. The HUD meter still shows the raw pressure: it is a live readout of the market and must not be flattered.
  - **The pair's rhythm was redrawn every phase.** circle/engage/break durations came from wide independent ranges (0.7-1.6 s, 0.9-2.1 s, 0.35-0.85 s). They are now shares of one **bar** whose length only breathes with the smoothed tape temperature (1.55-1.9 s): close in for 26% of the bar, break 13%, circle 61%, with +/-8% jitter. The gap targets also moved closer together (1.60 / 2.06 / 2.24, was 1.45 / 1.95 / 2.32) because a wide swing in the pair's distance read as the two of them lunging and retreating at each other.
  - **The attack clock queued instead of beating.** It asked "has it been long enough since the last move?" and retried every frame, so attacks blocked by a blade lock piled up and fired together the moment the lock ended. Attribution (the director now logs every start with its reason: tempo / spike / momentum / extreme / reaction) put a CV of **0.68** on the tempo stream alone and showed the bursts landing right after locks. The clock now owns a **phase**: each slot is scheduled when the previous one comes due, and a slot the fighters cannot use is skipped, never saved. The engage/circle tempo contrast also softened (0.56 / 0.80, was 0.44 / 0.92) so the pair does not lurch from a flurry to a lull.
  - **The blades locked on a cadence guard.** The odds were a flat coin flip (0.6 with a parry in progress, 0.18 without), and the first thing the measurement did was contradict the assumption behind the fix: the lock *spacing* was already regular (5.4-6.6 s between locks), it was the arrival pattern that could run hot or cold. So the odds went back to the originals and gained a guard instead (0.35x within 4 s of the last lock, 1.25x once it is 9 s overdue), which keeps the ~6 s cadence while removing both the pile-ups and the droughts. The first attempt at "steadier" hit the rate with a ramping probability and halved the highlights from 5.7 to 2.75 per run, which is a worse fight, so it was reverted on the numbers. The lock's own duration tightened to 0.42-0.52 s, and on a flat tape the winner alternates instead of being drawn.

  - **The camera and the crowd chased a binary flag.** "Is either cat locked" flips on every state change, so the heat (0.55/s decay, 1.2/s rise) and the push-in (chased at dt*2.6) pumped at ~2 Hz. The director now publishes one **continuous brawl intensity** (a per-state weight, low-passed at 0.7 s) and both follow that; the per-hit hit-stop is gone as well (it fired twice a second and read as a stutter, so only the deliberate blade-lock clash keeps its slow-mo).
  - **The plates were labelling the plumbing.** RECOVER and IDLE are not fight events, so they now need to settle (0.18 s / 0.30 s) before they reach the HUD, and every label holds a 180 ms dwell; CLASH, HIT and BLADE LOCK still show the instant they happen.
  - **Result (3 x 34 s runs per side: the v12 commit checked out into a worktree with `git worktree add`, against the working tree, same seed, `tools/steadiness.mjs`):** camera push-in sd 0.151 -> 0.043 (-72%), its travel 0.515 -> 0.192 (-63%) and its direction reversals 0.314/s -> 0.108/s (-66%); crowd heat sd 0.340 -> 0.158 (-53%) with its 10th percentile 0.00 -> 0.39, so the crowd no longer dies between beats; 2-second attack bursts sd 2.07 -> 1.95 (-6%) and their CV 0.61 -> 0.53 (-13%). The fight itself is untouched: 1.91 -> 2.05 attacks/s, a blade lock every 6.10 s -> 5.89 s (5.7 -> 5.0 per run), clashes 5.0 -> 4.7 per run. The honest nulls: the interval between consecutive attacks is a superposition of two independent fighters, so its CV barely moves (1.27 -> 1.25) and the plate label churn is unchanged (1.52 -> 1.50 /s) because it follows the fight, which is just as busy.

